#include <stdio.h>
#include <string.h>

int main() {
    char str1 = "";
    char str2 = "";
    memmove(str + 7, str + 0, 3); // Mueve "Hello" a la posición 7

    printf("Resultado: %s\n", str);

    return 0;
}